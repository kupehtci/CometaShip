#version 330 core

void main()
{
    // Fragment shader is empty because we only need depth information
    // The depth values are automatically written to the depth buffer
    // No need to write any color output
}